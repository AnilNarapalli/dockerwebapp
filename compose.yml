version: "3.8"

services:
  devopsdb:
    image: anilkumarnarapalli/project:db
    ports:
      - "3306:3306"
    deploy:
      replicas: 3
    networks:
      - mynetwork

  application:
    image: anilkumarnarapalli/project:app
    ports:
      - "1111:8080"
    deploy:
      replicas: 3
    networks:
      - mynetwork
    depends_on:
      - devopsdb

networks:
  mynetwork:
    driver: overlay
  
